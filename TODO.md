# TODO: Diagnose & Fix UI Issue on Deployed Site

- [ ] Verify REACT_APP_API_BASE_URL environment variable is set correctly on Netlify.
- [ ] Confirm backend API endpoints are reachable from deployed frontend (check network requests).
- [ ] Check Netlify build and publish directory settings (publish = frontend/build).
- [ ] If needed, update frontend/src/api/client.js or environment variables for correct API base URL.
- [ ] Inspect browser console and network tab on deployed site for errors.
- [ ] Apply code/config fixes and redeploy.
- [ ] Verify UI renders correctly on deployed site.
